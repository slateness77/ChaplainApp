<%= form_for @employee do |f| %>
  Name: <%= f.text_field :name %><br />

  <%= collection_check_boxes :employee, :available_day_ids, AvailableDay.all, :_id, :to_s %>

  <br />
  <%= f.submit %>
<% end %>
